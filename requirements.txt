# ── Core runtime ──────────────────────────────────────────────────────────────
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.2.25
charset-normalizer==3.4.6
exceptiongroup==1.3.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
jsonpatch==1.33
jsonpointer==3.0.0
langchain-core==0.3.83
langgraph==0.6.11
langgraph-checkpoint==2.1.2
langgraph-prebuilt==0.6.5
langgraph-sdk==0.2.9
langsmith==0.4.37
orjson==3.11.5
ormsgpack==1.11.0
packaging==25.0
pydantic==2.12.5
pydantic_core==2.41.5
PyYAML==6.0.3
requests==2.32.5
requests-toolbelt==1.0.0
tenacity==9.1.2
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
uuid_utils==0.14.1
xxhash==3.6.0
zstandard==0.25.0

# ── Semantic-evaluation layer (Setup A: pre/post refinement BERTScore) ────────
# Pinned for reproducibility of `semantic.bertscore_f1.*` in report.json.
# The module loads lazily — pipeline still runs if this is uninstalled.
bert_score==0.3.13

# ── Optional: BLEURT (secondary semantic metric) ──────────────────────────────
# Pinned for reproducibility of `semantic.bleurt.*` in report.json.
# The module loads lazily; pipeline still runs and the BLEURT block in the
# report degrades to a structured `error` shape when this is uninstalled.
# Uses the BLEURT-20-D12 distilled checkpoint via the lucadiliello PyTorch port.
bleurt-pytorch==0.0.1
# protobuf is a hidden runtime requirement of bleurt-pytorch's tokenizer
# (BleurtTokenizer falls back to a sentencepiece-via-protobuf loader when
# the vocab file is unavailable).  Pinning it avoids the cryptic
# "PROTOBUF_IMPORT_ERROR" on first call.
protobuf>=6.0.0
